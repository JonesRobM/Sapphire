# Sapphire Restoration Plan

_Drafted 2026-08-28. Status legend: ☐ todo · ◐ in progress · ☑ done · ✋ needs Rob (git / decision)._

Sapphire is the post-processing environment for metallic-nanoparticle MD written
during Rob Jones's PhD (Baletto group, KCL). Goal of this plan: make it install,
import and run cleanly on modern Python again, shrink the repository, and leave
it in a state where further science can be added with confidence.

Ground rules for the work:

* **No git commands are run by Claude.** Every phase ends with a list of files
  changed and the exact git commands for Rob to run.
* Behaviour-preserving first. Phases 1–3 change *no* numerical results; they
  only fix imports, packaging and tree hygiene. Bug fixes that could change
  output are isolated in Phase 4 and each one is noted.
* Nothing scientific is deleted. Stagnant code is moved to `legacy/`, not
  removed, so it can be resurrected when its purpose is remembered.

---

## Phase 1 — Repository hygiene  ☑ files done 2026-08-28 / ✋ commit

Remove tracked artefacts that `.gitignore` already forbids, plus stray files.

| Item | Action |
|---|---|
| `build/`, `main/build/`, `main/dist/`, `main/.eggs/` | delete (regenerated by any build) |
| `main/Sapphire.egg-info/`, `main/Sapphire/Sapphire.egg-info/` | delete |
| all `__pycache__/`, `*.pyc`, `.ipynb_checkpoints/` | delete |
| `HEAD`, `config`, `description` (root) and `main/HEAD`, `main/config`, `main/description`, `main/hooks/`, `main/info/` | delete — a copied `.git/` internals dump |
| `Logs/~$DocStrings.xlsx` | delete (Excel lock file) |
| `README` (plain) | delete; `README.md` is canonical; `setup.py` long_description repointed |
| `main/README`, `main/setup.py` | delete (duplicates of root files) |
| `Raffy` submodule (empty, never initialised) | remove from `.gitmodules`; note optional install in docs |
| `.gitignore` | add `Logs/~$*`, `*.traj`, `main/Sapphire/Tutorials/Data/*.tar.gz` |

Expected effect: ~300 files / ~4.5 MB removed from the working tree.

## Phase 2 — Make the package importable  ☑ done 2026-08-28 (all modules import; pyGDM2 optional)

1. `Light/Epsilon_DFT.py:436` — the Ni Johnson/Christy table was pasted as
   tab-separated text inside `np.array([...])`. Reformat to a proper array.
2. Convert 24 implicit-relative imports to absolute `Sapphire.*` imports
   (`from CNA import Utilities` → `from Sapphire.CNA import Utilities`, etc.).
   Files: `Process.py`, `IO/*.py`, `CNA/{FramePattern,Model}.py`,
   `Graphing/{Plot_input,Reader,Plotter}.py`, `Light/{__init__,ClassSpec}.py`,
   `Potentials/Potentials.py`.
3. `CNA/Model.py` — `from sklearn.externals import joblib` → `import joblib`.
4. Quarantine stagnant modules to `legacy/` (kept in tree, not packaged):
   `Emerald/` (a script masquerading as a module — 232 undefined names),
   `CNA/main.py` (imports non-existent `cna_signatures`, `cna_patterns`, `xyz_io`),
   `CNA/FrameSignature_Ovito.py` (ovito-only duplicate of `FrameSignature.py`),
   `IO/ExtendXYZ.py` (byte-identical to `Utilities/ExtendXYZ.py`).
5. Replace glob-generated `__all__` in every `__init__.py` with explicit exports.
6. Acceptance: `python -c "import Sapphire, Sapphire.Process, Sapphire.Post_Process, Sapphire.CNA, Sapphire.IO, Sapphire.Graphing, Sapphire.Utilities, Sapphire.Potentials"` succeeds in a fresh venv.

## Phase 3 — Packaging  ☑ done 2026-08-28 (`pyproject.toml`, extras, ruff, pre-commit)

* Replace `setup.py` with `pyproject.toml` (setuptools backend, `src`-style
  layout kept as `main/` to avoid churn; `package_dir` preserved).
* `requires-python = ">=3.10"`.
* Core deps: `numpy`, `scipy`, `ase`, `pandas`, `networkx`.
* Extras: `cna = [ovito]`, `changepoint = [ruptures]`, `ml = [scikit-learn, joblib]`,
  `light = [pygdm2]`, `plot = [matplotlib, seaborn]`, `quote = [wikiquote]`,
  `dev = [pytest, ruff]`, `all`.
* Dropped from hard requirements: `tensorflow`, `mir-flare`, `ray`, `numba`,
  `sklearn` (blocked PyPI name), duplicate `networkx`. They are recorded in
  `docs/DEPENDENCY_NOTES.md` as "intended, never wired in" so the intent survives.
* `wikiquote` start-up quote is **kept** (Rob's request) — already guarded by
  `try/except ModuleNotFoundError`, becomes the `quote` extra.
* Add `ruff` config (pyflakes + pycodestyle rules) and a `pre-commit` config.

## Phase 4 — Bug fixes + tests  ☑ done 2026-08-28 — 70 tests pass, 0 swallowed exceptions on Au561; see `docs/CHANGELOG.md`

Each fix gets a one-line note in `docs/CHANGELOG.md`. Confirmed defects:

| File:line | Defect |
|---|---|
| `Post_Process/Stats.py:56-57` | `P`, `Q` undefined in divergence function |
| `Post_Process/Mass_Activity.py:70` | `mass_NP` undefined; `numpy` imported twice |
| `Graphing/Reader.py:84-94` | `Metadata` undefined |
| `Graphing/Reader.py:488` | `Quant.lower is 'masterkey'` — method compared to str with `is` |
| `Graphing/Plot_Funcs.py:434-436` | `is` used for string equality |
| `Graphing/Plot_Funcs.py:175,405-407` | `ax`, `ax2Ticks`, `tick_function` undefined |
| `Graphing/Plotter.py:362-386` | dead block with 9 undefined names |
| `CNA/Model.py:190-273` | 7 undefined names in classifier glue |
| `Utilities/ExtendXYZ.py:141-166` | `count`, `AuNN`, `PtNN`, `PtInfo` undefined |
| 76 bare/broad `except:` | narrow or log; never swallow silently |
| 99 `print()` | route through `logging` |

Tests (`tests/`, pytest):
* `test_imports.py` — every public module imports.
* `test_process_smoke.py` — run `Process` on `Tutorials/CNA/Au561.xyz`
  (561-atom Au icosahedron, 78 KB) for a handful of quantities.
* `test_distfuncs.py`, `test_adjacent.py` — numerical checks against tiny
  hand-built clusters (dimer, tetrahedron, 13-atom icosahedron).
* GitHub Actions workflow: lint + tests on 3.10–3.13.  ✋ (needs `.github/` push)

## Phase 5 — Shrink history (721 MB `.git`)  ◐ helper + samples + runbook done; ✋ Rob runs `docs/HISTORY_REWRITE.md`

Root cause: `main/Sapphire/Tutorials/Data/{AgNi,AuPd,AuPt,CuNi}.tar.gz`
(4 × 58 MB; each holds two ASE `.traj` binaries of ~48 MB) plus historic
`.egg`/`build/` churn.

Data strategy (decided 2026-08-28):
1. **Cannot be regenerated deterministically** — they are stochastic 14 ns MD
   runs. Keep the originals, but *outside* git history.
2. Host the four tarballs as a **GitHub Release asset** (`data-v1`) — free, no
   LFS quota. `Sapphire.Tutorials.data.fetch("AuPt")` downloads + caches to
   `~/.cache/sapphire/` with a SHA-256 check.
3. Ship a **down-sampled sample** in-repo: every 20th frame of one simulation
   per system, written as compressed extxyz (~1–2 MB each) so tutorials run
   offline.
4. Offer `Sapphire.Tutorials.data.synthetic(...)` — ASE + EMT/Gupta short MD on
   a small cluster, seeded, for smoke tests that need a "trajectory-shaped"
   input without any download.
5. History rewrite with `git filter-repo` on a fresh clone; commands will be
   listed verbatim in `docs/HISTORY_REWRITE.md` for Rob to run. Expected
   result: `.git` < 10 MB.

## Phase 6 — file-backed metadata + strictness  ☑ done 2026-08-28

Decision (Rob, 2026-08-28): files under the run directory are the source of truth; analysis
tools read them back. Goal: make Sapphire usable on output from general-purpose MNP codes.

* `IO/Reader.py` maps `OutputInfo*` tables → arrays; `Process.load_metadata()` uses it.
* `analyse()`, `write_meta()`, extended-xyz rewired; `Process(strict=True)`; `logging`.
* Follow-ups: `Graphing.Read_Meta` still expects the old `Metadata.csv` pickle layout — port it
  to `Reader`; document the `Time_Dependent/` file contract for external producers.

---

## Open items carried forward
* `Emerald/` and `CNA/main.py` intent — Rob doesn't recall; parked in `legacy/`.
* Whether `mir-flare` / `tensorflow` / `ray` still belong in the roadmap.
* `Logs/DocStrings.xlsx` — likely a docstring audit sheet; keep or convert to md?
* `Graphing.Read_Meta`/`Plot_Funcs` port to `IO.Reader` (only `Get_Heights_Ovito` is tested so far).
* Remaining broad `except:` blocks outside `Process` (Adjacent, Kernels, DistFuncs) could route via a shared reporter.
